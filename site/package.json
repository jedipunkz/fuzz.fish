{
  "name": "fuzz-fish-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-assets": "mkdir -p public/assets && cp ../assets/fuzz.png ../assets/fuzz.gif public/assets/",
    "predev": "npm run sync-assets",
    "dev": "astro dev",
    "prebuild": "npm run sync-assets",
    "build": "astro build",
    "prepreview": "npm run sync-assets",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.0.4"
  }
}
